import React from "react";
import { View, Text, StyleSheet } from "react-native";

const EditInfoScreen = (props) => {
    return (
        <View>
            <Text>This is EditInfoScreen</Text>
        </View>
    );
};
const styles = StyleSheet.create({});

export default EditInfoScreen;
